<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upc.dao.RoleDao">

    <resultMap type="com.upc.model.Role" id="RoleMap">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoleMap">
        select
          id, role_name
        from springmvc.role
        where id = #{id}
    </select>

    <select id="queryRolesByUserId" resultMap="RoleMap">
        select
               r.id, r.role_name
        from springmvc.role r
        INNER JOIN user_role ur ON r.id=ur.role_id
        where ur.user_id = #{userId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoleMap">
        select
          id, role_name
        from springmvc.role
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="RoleMap">
        select
          id, role_name
        from springmvc.role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="roleName != null and roleName != ''">
                and role_name = #{roleName}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into springmvc.role(role_name)
        values (#{roleName})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update springmvc.role
        <set>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from springmvc.role where id = #{id}
    </delete>

</mapper>